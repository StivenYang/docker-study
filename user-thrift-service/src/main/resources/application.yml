server:
  port: 7911

spring:
  application:
    name: user-thrift-service
  datasource:
    url: jdbc:mysql://localhost:3306/db_user?useSSL=false&useUnicode=true&characterEncoding=utf8&autoReconnect=true&failOverReadOnly=false
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

